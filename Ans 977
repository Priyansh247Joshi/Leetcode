class Solution {
public:
    vector<int> sortedSquares(vector<int>& A) 
    {
        int n=A.size();
        vector<int> ans(n,0);
        for(int i=0;i<n;i++)
            ans[i]=A[i]*A[i];
        sort(ans.begin(),ans.end());
        return ans;
        
    }
};
